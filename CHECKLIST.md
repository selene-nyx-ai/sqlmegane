# 本番 SQL の実行前チェックリスト（手作業運用向け）

本番データベースに手作業で UPDATE / DELETE / TRUNCATE などを流す前に、上から順に確認するためのリストです。チームの運用手順にそのままコピーして使ってください（MIT ライセンス。改変自由）。

各項目は「ツールが無くてもできること」を基本にしています。[SQLMegane（SQLめがね）](https://selene-nyx-ai.github.io/sqlmegane/) を使うと、手順 1 の日本語要約、手順 4 の検算 SELECT の作成、手順 5 のうち SQL 文面上のトランザクション確認を補助できます。DB 上での件数確認や自動コミット設定・影響行数の確認は、ツールでは代わりにできません。

## 実行前

### 1. これから流す SQL を「一文の日本語」で言えるか

- [ ] 「`m_users` のうち、最終ログインが 2024 年より前の行の `status` を INACTIVE にする」のように、**対象テーブル・対象範囲・何をするか**を一文で言える
- [ ] 言えない場合は、言えるまで SQL を読み直す。言えない SQL は流さない

### 2. 対象範囲（WHERE 句）

- [ ] UPDATE / DELETE に WHERE 句がある（無い場合は「全件が対象」であることを声に出して確認した）
- [ ] デバッグ用の `WHERE 1=1` や `OR 1=1` が残っていない
- [ ] `OR` と `AND` が混在する場合、括弧で優先順位を明示している
- [ ] JOIN を伴う UPDATE / DELETE で、**実際に書き換わるテーブル**がどれか分かっている
- [ ] LEFT JOIN の右側テーブルの列に、NULL 補完行を除外する WHERE 条件（`右表.col = ...` や `IS NOT NULL` など）を置いていない（LEFT JOIN が INNER JOIN 相当になり対象が減る。必要なら条件を ON 句へ移す。`IS NULL` で未結合行を抽出する意図的な使い方は除く）
- [ ] サブクエリ付き `IN (...)` / `NOT IN (...)` の場合、サブクエリ単体を先に実行して結果を見た（`NOT IN` は右辺に NULL が含まれると非一致行でも UNKNOWN になり、WHERE では通常すべて除外される）

### 3. DELETE と TRUNCATE の取り違え

- [ ] `TRUNCATE` を書いている場合、それが意図どおり（DELETE ではなく TRUNCATE）である
- [ ] 通常テーブルの `TRUNCATE` は Oracle / MySQL では原則として暗黙コミットされ、ROLLBACK できない（一時表など製品固有の例外は資料で確認）。PostgreSQL / SQL Server では未コミットのトランザクション内でのみ戻せる。**「取り消せない操作」として扱う**

### 4. 検算 SELECT（件数）

- [ ] UPDATE / DELETE と**同じ WHERE 句**で `SELECT COUNT(*)` を流し、件数が想定と合う
- [ ] DELETE の場合、テーブルの総行数（`SELECT COUNT(*) FROM テーブル`）も控えた。「削除件数 = 総行数」なら止まる
- [ ] 件数だけでなく、数行を `SELECT *` で見て「消してよい／変えてよい行」であることを目視した
- [ ] 件数は確認時点の目安と理解している（同時更新で変わる）。実行後の影響行数と突き合わせる

### 5. トランザクション

- [ ] 作業の最初に、自動コミットの設定と**進行中のトランザクションが無いこと**を確認した（MySQL では進行中のトランザクションに `BEGIN` を重ねると、それまでの変更が暗黙にコミットされる）
- [ ] `BEGIN`（MySQL / PostgreSQL）／ `BEGIN TRANSACTION`（SQL Server）を**明示して**から流す。Oracle は最初の DML で自動開始（クライアントの自動コミットは無効にする）
- [ ] SQL*Plus を使う場合、`SHOW AUTOCOMMIT` と `SET EXITCOMMIT` の状態を確認した（`EXITCOMMIT` は既定 ON で、AUTOCOMMIT が OFF でも通常終了時に未コミットの変更がコミットされる）。終了前に明示的に `COMMIT` か `ROLLBACK` する
- [ ] MySQL は既定が `autocommit=1`。psql も既定は自動コミット。**BEGIN を打っていなければ実行した瞬間に確定する**と理解している
- [ ] `COMMIT` を同じブロックに書いて一括実行しない（影響行数を見る前に確定する）
- [ ] 実行後に影響行数（`Rows matched` / `rows affected` / `UPDATE n`）を見てから `COMMIT` する。想定外なら `ROLLBACK`
- [ ] 複数の破壊的操作を 1 回で流す場合、1 文ずつ流せないか検討した

### 6. 実行環境

- [ ] 接続先が本番であることを再確認した（ホスト名・DB 名・プロンプトの色など）
- [ ] GUI ツールで**範囲選択して実行**する場合、選択範囲に WHERE 句まで含まれている（行の途中で切れていない）
- [ ] 大量行を更新／削除する場合、ロック時間とレプリケーション遅延への影響を考えた（LIMIT や分割実行を検討）

## 退避してから変更する（退避 → 変更 → 補償）

- [ ] 新規の空退避表・明示列・不変で非 NULL の一意キー・接続設定を確認し、A と B の全件数およびキー集合を照合した
- [ ] B〜D は同じ接続で段階別に実行し、エラーは ROLLBACK、接続喪失・COMMIT 応答不明は結果不明として停止した
- [ ] 補償は完全復元ではないと理解し、全退避集合の存在／不在・期待値・即時一意制約を検査し、記録と退避表の保管期限を決めた

## 実行後

- [ ] 影響行数が検算 SELECT の件数と一致した
- [ ] `COMMIT` した時刻と実行した SQL を、そのまま記録に残した（事故時の復旧に必要）

## 事故が起きたとき

- [ ] 追加の UPDATE / DELETE で「直そう」としない（状況が悪化する）
- [ ] 事故の時刻・実行した SQL・影響行数を正確に記録する
- [ ] 定められたインシデント手順で DBA／責任者に連絡し、書き込み停止や遅延レプリカの適用停止が必要かを判断してもらう（停止の判断自体が可用性と復旧可能性に影響する）
- [ ] 復旧手段（バックアップ / PITR / 遅延レプリカ / binlog・WAL）が**事故前に**用意されているかは、平時に確認しておく

---

このチェックリストは [SQLMegane](https://github.com/selene-nyx-ai/sqlmegane) の一部として公開しています。項目の追加・修正の提案は Issue / Pull Request / [Discussions](https://github.com/selene-nyx-ai/sqlmegane/discussions) で歓迎します。
